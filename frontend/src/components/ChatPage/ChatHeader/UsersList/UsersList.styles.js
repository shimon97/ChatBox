import { AvatarGroup } from "@mui/material";
import styled from "styled-components";

export const Title = styled.b`
    font-size: 20px;
    cursor: pointer;
`;

export const AvatarsList = styled(AvatarGroup)`
    margin-left: 20px;
    cursor: pointer;
`;
